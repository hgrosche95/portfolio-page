---
title: "Great Galguti Game"
summary: "Echtzeit-Multiplayer-Kartenspiel für 3–8 Spieler:innen — mit Nutzerkonten, serverseitig validierten Zügen und Live-Spielstand über mehrere Clients hinweg."
githubRepo: "great_galguti_game"
techStack: ["TypeScript", "React", "Node.js", "WebSocket", "PostgreSQL", "Prisma", "JWT", "Vitest", "Docker", "Azure", "GitHub Actions"]
order: 1
kind: fullstack
liveUrl: "https://blue-sea-08b19cb0f.7.azurestaticapps.net/"
liveLabel: "Jetzt spielen"
liveStatusCheck: true
architecture:
  lanes: ["Clients", "Server", "Daten"]
  nodes:
    - id: web
      label: "React 19"
      kind: frontend
      sub: "dein Browser"
      lane: 0
      row: 0
      description: "Der Spieltisch. Bekommt nur die eigene Hand und die Kartenanzahl der Gegner, nie deren Karten."
    - id: others
      label: "Weitere Clients"
      kind: frontend
      sub: "3–8 am Tisch"
      lane: 0
      row: 1.3
      description: "Alle anderen Spieler:innen. Jede Änderung kommt per WebSocket an."
    - id: server
      label: "Express + WebSocket"
      kind: backend
      sub: "Auth · Rate-Limit"
      lane: 1
      row: 0
      description: "Nimmt Züge entgegen, prüft sie gegen die echte Hand und verteilt den neuen Stand. Auth per Handshake mit Timeout, weil Browser beim WS-Handshake keine Header setzen können."
    - id: logic
      label: "Spiellogik (src/)"
      kind: backend
      sub: "ohne Abhängigkeiten"
      lane: 1
      row: 1.3
      description: "Reine Funktionen, mit Vitest getestet. Server, Bots und Tests nutzen exakt denselben Code."
    - id: db
      label: "PostgreSQL · Prisma"
      kind: data
      sub: "Neon"
      lane: 2
      row: 0
      description: "Nutzerkonten."
  edges:
    - [web, server]
    - [server, logic]
    - [server, db]
    - [server, others]
  scenarios:
    - name: "Zug spielen"
      steps:
        - { from: web, to: server, label: "Zug", detail: "per WebSocket gesendet" }
        - { from: server, to: logic, label: "Validieren", detail: "gegen die echte Hand, nicht gegen den Client" }
        - { from: logic, to: server, label: "gültig" }
        - { from: server, to: others, label: "Broadcast", detail: "neuer Stand, ohne fremde Karten" }
        - { from: server, to: web, label: "Update", detail: "eigene Hand aktualisiert" }
    - name: "Anmelden"
      steps:
        - { from: web, to: server, label: "Login", detail: "Rate-Limit gegen Passwort-Raten" }
        - { from: server, to: db, label: "Konto prüfen" }
        - { from: db, to: server, label: "ok" }
        - { from: server, to: web, label: "JWT" }
        - { from: web, to: server, label: "WS-Handshake", detail: "Token als erste Nachricht, mit Timeout" }
---

Ein Echtzeit-Multiplayer-Kartenspiel für 3–8 Spieler:innen (Stichspiel mit Zahlenkarten und Jokern), gebaut, um mein TypeScript- und Full-Stack-Wissen an einem praktischen individuellen Projekt zu vertiefen. Es beinhaltet echte Nutzerkonten und einen Live-Zustand über mehrere Clients hinweg.

<div class="screenshot-grid">
  <img src="/projects/great-galguti-game/login.webp" alt="Login-Screen" />
  <img src="/projects/great-galguti-game/gameplay.webp" alt="Laufende Partie mit Bots" />
</div>

**Warum so gebaut:** Das Repo ist ein Monorepo aus drei unabhängigen Teilprojekten: reine Spiellogik (`src/`, ohne Laufzeit-Abhängigkeiten, mit Vitest getestet), ein Node/Express-Server mit WebSockets für den Live-Betrieb sowie Prisma/PostgreSQL für Accounts, und ein React-19-Frontend. Der Server importiert die Spiellogik aus `src/` direkt, statt sie zu duplizieren. Das heißt, dieselben Funktionen entscheiden, ob ein Zug gültig ist, egal ob im Test, im Bot oder live im Spiel.

**Technisch interessant:** Der Server validiert jeden Zug serverseitig gegen die tatsächliche Hand des Spielers, statt dem Client zu vertrauen. Das schützt vor Betrug mittels eines manipulierten WebSocket-Clients. Jeder Client bekommt außerdem nur seine eigene Hand plus die Kartenanzahl der Gegner übermittelt, aber nie deren tatsächliche Karten. Verdeckte Informationen werden also serverseitig durchgesetzt, nicht nur im UI versteckt. Auth über WebSocket läuft über ein Handshake-Muster mit Timeout, weil Browser beim WS-Handshake keine eigenen Header setzen können. Ungültige Nachrichten, ein Beitritt mitten in einer Partie oder ein Neustart-Befehl während des Spiels können den Server nicht mehr zum Absturz bringen; das decken eigene Robustheitstests ab. Die Auth-Routen haben ein Rate-Limit gegen Passwort-Raten.

**Deployment:** Frontend auf Azure Static Web Apps (inkl. PR-Preview-Umgebungen), der Server als Docker-Container auf Azure Container Apps (OIDC-Deploy statt gespeicherter Zugangsdaten), Datenbank auf Neon (verwaltetes Postgres). `main` ist geschützt und verlangt grünes CI (Typecheck + Tests über alle drei Teilprojekte) vor jedem Merge.
