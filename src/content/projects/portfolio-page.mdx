---
title: "Diese Portfolio-Seite"
summary: "Diese Seite selbst: Astro mit React-Islands, ein Node-Graph als Navigation und echte Deploy-Daten aus der eigenen Pipeline."
githubRepo: "portfolio-page"
techStack: ["Astro", "TypeScript", "React", "Tailwind CSS", "MDX", "Azure", "GitHub Actions"]
order: 6
kind: static
hosting: "Static Web Apps · Free Tier"
decisions:
  - topic: "Framework"
    rejected: ["Next.js", "Vanilla + Vite"]
    chosen: "Astro mit React-Islands"
    reason: "Fast alles ist statischer Inhalt. React lädt nur dort, wo es interaktiv wird."
  - topic: "Pipeline-Grafik"
    rejected: ["echter Live-Ticker"]
    chosen: "echte Daten, als Animation abgespielt"
    reason: "Deploys sind selten. Ein Ticker stünde meistens still."
  - topic: "Demo-Status"
    rejected: ["Fetch aus dem Browser"]
    chosen: "Azure Function, server-to-server"
    reason: "Im Browser blockt CORS, und eine Ablehnung sieht aus wie ein toter Server. Ziel-URLs kommen nur aus einer Allowlist."
  - topic: "Domain"
    rejected: ["DuckDNS", ".tk / .ml"]
    chosen: "is-a.dev"
    reason: "Kostenlos, aber gedacht für Entwickler-Portfolios, statt nach Heimserver oder Spam-Ruf auszusehen."
architecture:
  lanes: ["Build & Browser", "Azure", "Extern"]
  nodes:
    - id: build
      label: "Astro Build (static)"
      kind: backend
      sub: "MDX · Zod-Schema"
      lane: 0
      row: 1.3
      description: "Projekttexte als typisierte Content Collection. Ein Tippfehler im Frontmatter bricht den Build, statt live eine kaputte Seite zu erzeugen."
    - id: islands
      label: "React Islands"
      kind: frontend
      sub: "im Browser"
      lane: 0
      row: 2.6
      description: "Projektseiten laden kein JS-Bundle. React gibt es nur für zwei Inseln auf der Startseite, und erst beim Hineinscrollen."
    - id: scripts
      label: "Build-Skripte"
      kind: backend
      sub: "prebuild-Hook"
      lane: 0
      row: 0
      description: "Holen GitHub-Stats und Deploy-Info. Welche Repos abgefragt werden, leiten sie aus dem Frontmatter ab."
    - id: gh
      label: "GitHub API"
      kind: external
      sub: "Stars · Pushes"
      lane: 2
      row: 0
      description: "Liefert die Repo-Daten für die Projektkarten."
    - id: swa
      label: "Azure Static Web Apps"
      kind: external
      sub: "Free Tier · PR-Previews"
      lane: 1
      row: 1.3
      description: "Liefert die statische Seite aus. Jeder Pull Request bekommt eine eigene Vorschau-Umgebung."
    - id: func
      label: "Azure Functions"
      kind: backend
      sub: "Kontakt · Live-Status"
      lane: 1
      row: 2.6
      description: "Zwei kleine Funktionen: das Kontaktformular mit Honeypot und Rate-Limit, und der Erreichbarkeits-Check für die Demos."
    - id: brevo
      label: "Brevo"
      kind: external
      sub: "Mailversand"
      lane: 2
      row: 2
      description: "Verschickt die Nachrichten aus dem Kontaktformular."
    - id: demos
      label: "Demo-Backends"
      kind: external
      sub: "feste Allowlist"
      lane: 2
      row: 3.2
      description: "Die Container der Live-Demos. Ziel-URLs kommen nur aus einer festen Allowlist, nie aus dem Request."
  edges:
    - [build, islands]
    - [build, scripts]
    - [scripts, gh]
    - [build, swa]
    - [islands, swa]
    - [islands, func]
    - [func, brevo]
    - [func, demos]
  scenarios:
    - name: "Push auf main"
      steps:
        - { from: build, to: scripts, label: "prebuild" }
        - { from: scripts, to: gh, label: "Stats", detail: "für alle Repos aus dem Frontmatter" }
        - { from: gh, to: scripts, label: "Antwort" }
        - { from: scripts, to: build, label: "JSON", detail: "nach src/data" }
        - { from: build, to: build, label: "Zod", detail: "prüft jedes Frontmatter" }
        - { from: build, to: swa, label: "Deploy", detail: "per GitHub Actions" }
    - name: "Besuch + Demo-Check"
      steps:
        - { from: islands, to: swa, label: "Seite laden" }
        - { from: swa, to: islands, label: "HTML", detail: "Projektseiten ohne JS-Bundle" }
        - { from: islands, to: func, label: "live-status" }
        - { from: func, to: demos, label: "Probe", detail: "server-to-server, kein CORS" }
        - { from: demos, to: func, label: "Antwort" }
        - { from: func, to: islands, label: "wach / schläft", detail: "20 s Cache" }
    - name: "Kontaktformular"
      steps:
        - { from: islands, to: func, label: "Nachricht", detail: "+ Honeypot" }
        - { from: func, to: func, label: "Rate-Limit" }
        - { from: func, to: brevo, label: "Mail" }
        - { from: func, to: islands, label: "Gesendet" }
---

Die Seite, auf der Sie gerade sind. Sie ist dazu da, einen Teil meiner Skills zu präsentieren. Sie ist als Bewerbungsseite gebaut, sollte die Fähigkeiten aus dem Lebenslauf aber nicht nur behaupten, sondern vorführen: Der Node-Graph auf der Startseite ist die Navigation und gleichzeitig ein Beispiel für Systemintegration. Die Deployment-Grafik zeigt echte Daten aus dem letzten Deployment des Repos in der sich der Code dieser Seite befindet.

**Warum so gebaut:** Ursprünglich hatte ich reines Next.js geplant. Dann kam der Einfall, dass ja nicht alles durch React laufen muss und somit die Webseite performanter sein kann. Der Informationsteil ist ein reiner statischer Inhalt, nichts soll ablenken. Mit Next.js hätte ich den React-Runtime auch für Seiten ausgeliefert, die ihn nie brauchen. Deshalb Astro mit React-Islands. Die Projektseiten und das Impressum laden **kein einziges JavaScript-Bundle** — nur wenige Zeilen Inline-Skript für Theme-Umschalter und Scroll-Animation. React wird ausschließlich auf der Startseite für den Node-Graph und die Deployment-Grafik geladen, und dank `client:visible` auch dort erst, wenn die Komponenten tatsächlich ins Sichtfeld scrollen. React also nur da, wo es benötigt wird.

**Technisch interessant:** Die „Live"-Daten entstehen zur Build-Zeit, nicht im Browser. Zwei Skripte laufen über den npm-`prebuild`-Hook und schreiben JSON nach `src/data`: eines liest Commit, Branch und Zeitstempel aus den GitHub-Actions-Umgebungsvariablen (mit Fallback auf `git log`, damit es lokal genauso funktioniert), das andere holt Sterne, Sprache und letzten Push über die GitHub-API. Welche Repos abgefragt werden ist nirgends fest verdrahtet. Das Skript liest die Frontmatter aller Projekt-MDX-Dateien und leitet die Liste daraus ab. Es reicht also einfach eine neue Projektdatei anzulegen. Graph-Knoten, Unterseite und API-Abfrage entstehen von selbst. Genau so ist auch diese Seite hier hinzugekommen. Die Projekttexte liegen als typisierte Content Collection mit Zod-Schema vor: Ein Tippfehler im Frontmatter bricht den Build, statt live eine kaputte Seite zu erzeugen. Ein bewusst verworfener Ansatz war ein echter Live-Ticker für die Deployment-Daten. Deployments finden ja nicht durchgehend statt, ein „Live"-Widget stünde also meistens still. Stattdessen werden die echten Daten bei jedem Seitenaufruf als Animation abgespielt.

**Deployment:** Azure Static Web Apps auf dem kostenlosen Tarif, ausgelöst per GitHub Actions bei jedem Push auf `main`, inklusive Preview-Umgebungen für Pull Requests. Die Deployment-Grafik auf der Startseite zeigt nicht irgendeine Beispiel-Pipeline, sondern die Daten aus genau diesem Workflow-Lauf.
